# Project State

This file is the compact public-facing state of the Avalanche research program.

## Current Question

The project is no longer asking whether the apparatus can preserve memory at all.

It is asking:
- what kind of epistemic object the graveyard is
- whether a model can read the shape of its own eliminations
- whether that read can change live search rather than only offline interpretation

## Current Frontier

Active frontier:
- `V4.7` on top of the `V4.6` Orbit Parity negative result
- compression as working-memory intervention
- altitude as graveyard-reading intervention
- offline probes as a second-order instrument over accumulated state

Current live branch:
- `run-55` in `v47-phase2b`

Current read:
- negative-space altitude can move a run off pure neighbor logic
- it currently looks like one-ridge movement, not yet durable widening

## What Feels Settled

These are strong enough to treat as current project reality:

- Orbit Parity is a genuine ontology bottleneck for the scaffold.
- Score and ontology dissociate sharply.
- Compression and altitude are both real interventions, but neither is sufficient alone.
- The graveyard is not a flat failure log; it is a structured, path-dependent negative image.
- Read-only probes are now a first-class instrument once enough assembly exists.

## Key Witness Runs

- `run-12`
  compression competition and late suffix recovery
- `run-39`
  structural lifecycle: promotion, decay, tombstoning
- `run-51`
  saturated altitude baseline; graveyard richer than active theory
- `run-55`
  best live `2b` witness for negative-space altitude

## Key Mechanism Gaps

- The graveyard is more legible than usable.
- Cartography is ahead of navigation.
- The live system still appears ridge-limited.
- Narrative framing remains a measurement and prompting hazard.

## What Changed Recently

Major recent additions:
- `negative-space` altitude prompt mode
- `haiku_probe.py` and the read-only probe ladder
- synthetic graveyard probes
- chronology probes with thermodynamic metadata

Most important recent finding:
- targeted synthetic intermediate tombstones can extend the visible horizon of the graveyard toward cycle structure

Most important recent caution:
- qualitative progression markers get narrativized and can destroy measurement validity

## Where To Look Next

- [`docs/ARCHITECTURE.md`](ARCHITECTURE.md)
- [`docs/EXPERIMENT_INDEX.md`](EXPERIMENT_INDEX.md)
- [`INSTRUMENTATION.md`](../INSTRUMENTATION.md)
